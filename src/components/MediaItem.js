import React from 'react';
import { TouchableOpacity } from 'react-native';

const MediaItem = () => {
    return (
        <TouchableOpacity>

        </TouchableOpacity>
    );
};

export default MediaItem;